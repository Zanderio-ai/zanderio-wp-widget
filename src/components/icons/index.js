/**
 * Zanderio Widget — Icon Barrel Exports
 *
 * Re-exports all icon components so consumers can import from a single path:
 *   `import { CloseIcon, ChatIcon } from "../icons";`
 *
 * @module components/icons
 */

export { default as CloseIcon } from "./close-icon";
export { default as ChatIcon } from "./chat-icon";
